# Pinned runtime dependencies (required by the competition rules).
# Versions are those verified by the test suite on Python 3.14.
numpy==2.4.6
scipy==1.18.0
pandas==3.0.3
scikit-image==0.26.0
pycocotools==2.0.11

# Test + tooling
pytest==9.0.2

# Model training. Version pinned; the CPU/CUDA build tag is platform-specific
# (verified here with the CPU build for local dev/testing -- Kaggle notebooks
# resolve the matching CUDA build for the same torch==2.13.0 automatically).
torch==2.13.0

# Encoder pretraining and library segmentation heads, added when the
# architecture ablations in P4 begin (not required by the current model).
# timm
# segmentation-models-pytorch
# albumentations
